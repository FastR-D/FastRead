# Open Me First

Use this project on Windows with Docker Desktop.

1. Install and open Docker Desktop.
2. Double click `CHECK_REQUIREMENTS.bat`.
3. Double click `START_HERE.bat`.

When the browser opens, use:

```text
http://127.0.0.1:3015/
```

Stop the project with `STOP_HERE.bat`.
